import { Routes } from '@angular/router';
import { TDSSAMonitoringComponent } from './containers';

export const ROUTES: Routes = [
    {
        path: '',
        component: TDSSAMonitoringComponent,
    },
];
